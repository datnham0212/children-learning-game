import React from 'react';
import { Text, View, Image, StyleSheet, Platform } from 'react-native';

export function FruitGuessing() {
    return (
        <View>
            <View style={{ width: 300, height: 300, backgroundColor: 'green' }} />
        </View>
    );
}